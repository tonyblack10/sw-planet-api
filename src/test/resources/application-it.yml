spring:
  datasource:
    username: ${MYSQL_USERNAME:root}
    url: ${MYSQL_HOST:jdbc:mysql://localhost:3306/starwars?createDatabaseIfNotExist=true&serverTimezone=UTC}
    password: ${MYSQL_PASSWORD:mysql123}
  jpa:
    hibernate:
      ddl-auto: update
